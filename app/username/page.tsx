import UserName from "../_pages/UserName";

const page = () => {
  return (
    <div className="w-full min-h-screen py-10 flex flex-col items-center">
      <UserName />
    </div>
  );
};

export default page;
